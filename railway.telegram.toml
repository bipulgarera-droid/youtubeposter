# Telegram bot service for Railway
# Deploy this as a separate service pointing to the same repo

[build]
builder = "DOCKERFILE"
dockerfilePath = "./Dockerfile"

[deploy]
startCommand = "python execution/telegram_bot.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
